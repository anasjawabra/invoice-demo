{
  "name": "invoice-demo",
  "version": "1.0.0",
  "private": true,
  "description": "INTELLIBILL - Intelligent Invoice Management demo (static site)",
  "scripts": {
    "build": "echo \"No build step needed for this static site.\"",
    "start": "serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
